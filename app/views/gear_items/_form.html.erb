<%= form_with(model: gear_item) do |form| %>
  <% if gear_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(gear_item.errors.count, "error") %> prohibited this gear_item from being saved:</h2>

      <ul>
        <% gear_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_area :description %>
  </div>

  <div>
    <%= form.label :is_checked_out, style: "display: block" %>
    <%= form.check_box :is_checked_out %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
